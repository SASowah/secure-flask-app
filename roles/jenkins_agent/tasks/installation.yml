---
- name: Install Java (Jenkins agent dependency)
  become: yes
  command: dnf5 install -y java

- name: Create Jenkins User
  become: yes
  user:
    name: jenkins
    shell: /bin/bash
    create_home: true
    home: /home/jenkins

