---
- name: Install Java 17 (Jenkins agent dependency)
  become: yes #Ensure admin privileges
  yum:
    name: java-17-openjdk
    state: latest # Ensures updates

- name: Create Jenkins User
  become: yes
  user:
    name: jenkins
    shell: /bin/bash
    create_home: true
    home: /home/jenkins
